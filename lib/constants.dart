import 'package:flutter/material.dart';

import 'navigator_class.dart';

double fullWidth = MediaQuery.of(NavigationService.navigatorKey.currentContext!).size.width;
Color mainColor = Theme.of(NavigationService.navigatorKey.currentContext!).primaryColor;
Color textColor = Colors.white;